namespace FastCompute.ImageProcessing;

/// <summary>Identifies the transfer function used by stored channel values.</summary>
public enum ColorEncoding
{
    /// <summary>Standard nonlinear sRGB encoding.</summary>
    Srgb,

    /// <summary>Linear-light channel values.</summary>
    Linear
}
