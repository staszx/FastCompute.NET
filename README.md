# FastCompute.NET
FastCompute.NET is a high-performance .NET library for large-scale numerical computing with automatic backend selection. Write simple LINQ-like expressions, and FastCompute transparently executes them on the most efficient backend: Scalar CPU, Parallel CPU, SIMD, or GPU.
